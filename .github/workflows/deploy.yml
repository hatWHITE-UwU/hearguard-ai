# Despliegue: configura secretos (p. ej. RAILWAY_TOKEN) y servicios reales.
name: Deploy
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Configura Railway/Render y secrets antes de desplegar."
