{
  "name": "hearguard-bdd",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cucumber-js",
    "test:api": "cucumber-js --tags 'not @frontend and not @hardware and not @ai'"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.2.0",
    "jsonwebtoken": "^9.0.0",
    "mongodb-memory-server": "^10.0.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
